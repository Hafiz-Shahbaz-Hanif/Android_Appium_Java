# Overridable with -Dkey=value or environment variables (KEY with dots as underscores).

# Leave blank to start a local Appium server automatically; set to a URL to use
# an already-running server, a Grid, or a cloud provider.
appium.server.url=
appium.host=127.0.0.1
appium.port=4723

android.device.name=Android Emulator
android.platform.version=
android.automation.name=UiAutomator2

wait.explicit.seconds=20
wait.newcommand.seconds=120
